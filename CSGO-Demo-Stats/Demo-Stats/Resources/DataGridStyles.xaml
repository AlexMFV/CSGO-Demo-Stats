<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:Demo_Stats">

    <Style x:Key="DataGridDarkMode"
           TargetType="{x:Type DataGrid}"
           BasedOn="{StaticResource MetroDataGrid}">
        <Setter Property="Foreground" Value="{DynamicResource SoftWhite}"/>
        <Setter Property="IsReadOnly" Value="True" />
        <Setter Property="GridLinesVisibility" Value="Vertical" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="ClipToBounds" Value="True" />
        <Setter Property="Width" Value="Auto"/>
        <Setter Property="RowBackground" Value="{DynamicResource BackgroundGrey}"/>
        <Setter Property="FontWeight" Value="Regular"/>
        <Setter Property="AlternatingRowBackground" Value="{DynamicResource LighterGrey}"/>
    </Style>
        
    <Style x:Key="DataGridHeader" 
       TargetType="{x:Type DataGridColumnHeader}" 
       BasedOn="{StaticResource MetroDataGridColumnHeader}">
        <Setter Property="Background" Value="{DynamicResource BackgroundGrey}" />
        <Setter Property="Foreground" Value="{DynamicResource SoftWhite}"/>
    </Style>
</ResourceDictionary>